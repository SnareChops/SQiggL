machine:
  node:
    version: 0.12.0
deployment:
  production:
    branch: master
    commands:
      - git fetch
      - git checkout gh-pages
      - git merge master
      - git push origin gh-pages