machine:
  node:
    version:
      - '0.10.33'
      - '0.12.0'
      - 'iojs-v1.3.0'
deployment:
  production:
    branch: master
    commands:
      - git checkout gh-pages
      - git merge master
      - git push origin gh-pages